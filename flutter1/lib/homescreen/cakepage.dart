import 'package:flutter/material.dart';

class CakePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('cake page'),
        backgroundColor: Colors.pinkAccent,
      ),
    );
  }
}